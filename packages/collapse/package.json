{
  "name": "@vc-com/collapse",
  "version": "1.0.0",
  "description": "Vue 3 Collapse 组件",
  "files": [
    "dist",
    "package.json"
  ],
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "vite build && pnpm build:types",
    "build:types": "tsmc -p tsconfig.build.json",
    "prepublish": "pnpm build",
    "bump": "bumpp --release patch"
  },
  "devDependencies": {
    "@vc-com/util": "workspace:*",
    "@vc-com/motion": "workspace:*"
  },
  "peerDependencies": {
    "vue": "catalog:"
  }
}
