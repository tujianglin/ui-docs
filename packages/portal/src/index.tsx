import type { ContainerType, EscCallback, GetContainer, PortalProps } from './Portal';
import Portal from './Portal';
import { inlineMock } from './mock';

export { inlineMock };
export type { ContainerType, EscCallback, GetContainer, PortalProps };

export default Portal;
